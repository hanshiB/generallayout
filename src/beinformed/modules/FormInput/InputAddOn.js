// @flow
import React from "react";

/**
 * render Add On button for inputs
 */
const InputAddOn = ({ children }: { children?: any }) => (
  <div className="input-group-addon">{children}</div>
);

export default InputAddOn;
