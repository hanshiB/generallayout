CharIndex

    const Href = require('../../models/href/Href').default;

    <CharIndex
      enabled={['A', 'D', '&', '1']}
      active={['D']}
      href={new Href()}
      onSelect={() => {}}
    />