A ButtonGroup can be used to combine multiple buttons on a single line into a group

    const Button = require('./Button').default;

    <ButtonGroup>
      <Button>A</Button>
      <Button>B</Button>
      <Button>C</Button>
    </ButtonGroup>