The AddOnButton can be used on input groups to add a button next to a FormInput

    const Input = require('../FormInput/TextInput').default;
    
    <Input>
      <AddOnButton
        className=""
        type="submit"
        onBlur={() => {}}
        onClick={() => {}}
        onFocus={() => {}}
        onKeyDown={() => {}}
      >Go</AddOnButton>
    </Input>